 Text2Date←{
     ⍝ ⍵ ←→ Char mat of time strings
     ⍝ ⍺ ←→ Format [Century Window] [Century Anchor Year]
     ⍝ ← ←→ ⎕DT time number type 60 or 0 for invalid
     f←⊃⊆⍺
     k←+/'YMDhms'∊f
     Validate ⍺ AddCentury k Default k Scale f Decode f Parse↑⍵
 }
