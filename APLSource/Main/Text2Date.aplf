 Text2Date←{
     ⍝ ⍵ ←→ Char mat of time strings
     ⍝ ⍺ ←→ Format [Century Window]
     ⍝ ← ←→ ⎕DT time number type 60 or 0 for invalid
     f w←2↑50,⍨⊆⍺
     k←+/'YMDhms'∊f
     Validate w AddCentury k Default k Scale f Parse↑⍵
 }
