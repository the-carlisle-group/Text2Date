 toDateVarMethodA←{
     ⍝ Method A, must be very clean
     ⍝ Fast, minimal memory solution
     f dw←⍺
     r c←⍴⍵
     d←↑⍵[0;]~⎕D
     v←(r,3)⍴1↓1⊃d ⎕VFI,d,⍵
     n←⍉v[;⍋⍋f⍳'ymd']
     n[0;]←dw yearwindow n[0;]
     g←okdatemat n
     n←0 100 100⊥n
     n×g
 }
