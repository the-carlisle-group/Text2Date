 ProcessVariableA←{
     ⍝ ⍵ ←→ Character matrix of time strings
     ⍝ ⍺ ←→ Variable width format, Century Window
     ⍝ ← ←→ Decimal Encoded Type 60 time number
     f w←⍺
     r c←⍴⍵
     d←∪(⍵)~⎕D
     v←(r,3)⍴1↓1⊃d ⎕VFI,d,⍵
     n←⍉v[;⍋⍋f⍳'ymd']
     n[0;]←dw yearwindow n[0;]
     g←okdatemat n
     n←0 100 100⊥n
     n×g
 }
