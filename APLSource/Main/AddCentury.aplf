 AddCentury←{
     ⍝ ⍺ ←→ Century window
     ⍝ ⍵ ←→Decimal time number 60
     ⍝ ← ←→ ⍵ with added century
     y←⌊⍵÷10*4
     b←y<100
     ~∨/b:⍵
     ⍺>100:⍵+b×⍺×10*4
     s x←0 100⊤⍺-⍨⊃⎕TS
     c←b×s+y<x
     ⍵+c×10*6
 }
