 ParseFixed←{
     ⍝ ⍺ ←→ Fixed width format
     ⍝ ⍵ ←→ Character matrix of time strings
     ⍝ ← ←→ Time matrix
     f m←⍺ FixedMMM ⍵
     e←'YMDhms'
     c←1∊'YYYY'⍷f
     k←+/e∊f
     r←(k⍴100)*1+c∧'Y'=∪f∩e
     v←≠/2↑f
     b←v∨(1⊃⍴m)↑f∊e
     r⊤ToNum m/⍨b
 }
