 ParseFixed←{
     ⍝ ⍺ ←→ Fixed width format
     ⍝ ⍵ ←→ Character matrix of time strings
     ⍝ ← ←→ Time matrix
     f m←⍺ FixedMMM ⍵
     e←'YMDhms'
     c←1∊'YYYY'⍷f
     k←+/e∊f
     r←(k⍴100)*1+c∧'Y'=∪f∩e
     u←∧/f∊e
     b←u∨(1⊃⍴m)↑f∊e
     n←ToNum m/⍨b
     r⊤n×(~u)∨n<10*≢f
 }
